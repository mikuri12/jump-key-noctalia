/**
 * Adds JSX tags along with the custom tokenizer to the grammar
 * @param {any} grammar
 * @param {string} name
 */
export function addJsxTag(grammar: any, name: string): any;
export var space: string;
export var braces: string;
