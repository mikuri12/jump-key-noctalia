/**
 * @license @lucide/icons v1.28.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */

import buildLucideIconNode from './buildLucideIconNode.mjs';

const buildDomNode = ([tagName, attributes, children = []]) => `<${tagName} ${Object.entries(attributes).map(([attrName, value]) => `${attrName}="${value}"`).join(" ")}>${children?.map((child) => buildDomNode(child)).join("")}</${tagName}>`;
function buildLucideSvg(icon, params = {}) {
  return buildDomNode(buildLucideIconNode(icon, params));
}

export { buildLucideSvg as default };
//# sourceMappingURL=buildLucideSvg.mjs.map
