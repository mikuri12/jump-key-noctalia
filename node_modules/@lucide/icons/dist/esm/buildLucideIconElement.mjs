/**
 * @license @lucide/icons v1.28.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */

import buildLucideIconNode from './buildLucideIconNode.mjs';

const buildDomElement = (document, [tagName, attributes, children = []]) => {
  const element = document.createElementNS("http://www.w3.org/2000/svg", tagName);
  for (const [attrName, value] of Object.entries(attributes)) {
    element.setAttribute(attrName, value);
  }
  for (const node of children) {
    const childNode = buildDomElement(document, node);
    element.appendChild(childNode);
  }
  return element;
};
function buildLucideIconElement(document, icon, params = {}) {
  return buildDomElement(document, buildLucideIconNode(icon, params));
}

export { buildLucideIconElement as default };
//# sourceMappingURL=buildLucideIconElement.mjs.map
